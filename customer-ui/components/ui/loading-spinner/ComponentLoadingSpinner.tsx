import React from "react";
import classes from "./component-loading-spinner.module.css";

const ComponentLoadingSpinner: React.FC = () => {
  return <div className={classes.spinner}></div>;
};

export default ComponentLoadingSpinner;
