# Services
The Microservices that are used for the Commerce Hub ordering system

To build the project:

`npm run build`

To build the server:

`npm start`

To run es-linter the project:

`npm run lint`

To fix the errors encountered by linter:

`npm run lint:fix`